openapi: 3.0.3
info:
  title: Broken API
  version: 1.0.0
paths:
  /users/{id}:
    get:
      operationId: getUser
      responses:
        '200':
          description: ok
        # malformed below: unterminated flow sequence, invalid YAML
        bad: [unterminated, "missing close
