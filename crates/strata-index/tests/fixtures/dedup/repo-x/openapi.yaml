openapi: 3.0.3
info:
  title: Service X
  version: 1.0.0
paths:
  /users/{id}:
    get:
      operationId: getUser
      summary: Fetch one user via Service X
      responses:
        '200':
          description: ok
