openapi: 3.0.3
info:
  title: Service Y
  version: 1.0.0
paths:
  /users/{id}:
    get:
      operationId: getUser
      summary: Fetch one user via Service Y
      responses:
        '200':
          description: ok
